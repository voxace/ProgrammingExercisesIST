using System;

/*
===============================================================================
PROJECT 28: RANDOM NUMBERS
===============================================================================
Objective:
- Understand and practice generating random numbers in C#.
- Simulate dice rolls using the Random class and conditional statements.

Main Tasks:
1. Create a method named 'PromptUser' to prompt the user if they want to roll one or two dice.
2. Create a method named 'RollDice' to generate and return a random number between 1 and 6
3. If the user chooses to roll one die, call the 'RollDice' method once.
4. If the user chooses to roll two dice, call the 'RollDice' method twice and add the results together.
5. Print out the individual and total of the dice rolls.

Extension Tasks:
1. Add a feature to allow the user to choose the number of sides on the dice. 
2. Implement a loop to allow the user to continue rolling the dice or exit the program.
3. Add a feature to allow the user to choose the number of dice to roll.

Example: Generating a Random Number

    // Create a random number generator
    System.Random rnd = new System.Random();

    // Use the random number generator to create a random number between 1 and 10 (inclusive)
    int randomNum = rnd.Next(1, 11);

    // Output: The result will be a random number between 1 and 10.
    Console.WriteLine("Your number is " + randomNum);

===============================================================================
*/

namespace ProgrammingExercisesIST
{
    class Project_28_RandomNumbers
    {
        static void Main(string[] args)
        {
            // Your code here

        }
    }
}
/*
===============================================================================
FEEDBACK AREA: Paste your AI feedback here before submission:
===============================================================================



===============================================================================
*/